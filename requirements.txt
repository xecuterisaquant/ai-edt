# HTTP client for Ollama API
requests>=2.31.0

# RSS feed polling
feedparser>=6.0.11

# YAML config loading
pyyaml>=6.0

# Environment variable support (.env files)
python-dotenv>=1.0.0

# Google Gemini API (Stage 3 reasoning provider)
# Uses the official google-genai SDK (successor to google-generativeai)
google-genai>=1.0.0

# Phase 3: Outcome Tracking — post-signal price fetch
yfinance>=0.2.50