# Copy this file to .env and fill in any overrides.
# The application works without a .env file — all defaults are in config/settings.yaml.

# Override the Ollama host if running Ollama on a different machine or port.
# OLLAMA_HOST=http://localhost:11434
