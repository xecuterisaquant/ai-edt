# Copy this file to .env and fill in real values. Never commit the actual .env file.
# All defaults are in config/settings.yaml.

# Required when reasoning_provider = "gemini" (default)
# GEMINI_API_KEY=your_key_here

# Override the Ollama host if running Ollama on a different machine or port.
# OLLAMA_HOST=http://localhost:11434
